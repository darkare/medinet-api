This is the backend API program that is required for the UI to connect.

Before you start the program, it is assumed you have a Postgres DB running and have the credential for the admin postgres user.

You MUST configure the connection string located at the 